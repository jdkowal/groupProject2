<div class="row">
    <h4>My Coupons</h4>
</div>
<div class="collection">
    {{#each coupons as |coupon| }}
    <a href="#!" class="collection-item">{{coupon.medication_name}}</a>
    {{/each}}
</div>